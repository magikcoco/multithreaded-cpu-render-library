// src/renderer.c

#include "renderer.h"
#include <stdio.h>

void render(const char* asset, int x, int y) {
    // Rendering logic here
    printf("Rendering asset: %s at coordinates (%d, %d)\n", asset, x, y);
}